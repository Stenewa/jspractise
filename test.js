//var thisVar = "Lorem ipsum"
//console.log(thisVar)
// enter the word node followed by the file name in order to run on node compiler*v8 engine*
//use the plus sign to add vars to string
const thisConst = (cig) => {
    console.log('HEYYY SO CAN WE CIG AFTER S AND '+ cig);   
}

thisConst('CHILL');